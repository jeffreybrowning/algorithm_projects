def bucketsort(l):  # this is also called bin sort
    """Bucketsort has the following proceedure:

        1.  Setup array of empyy buckets
        2.  Go over original array and put every object in its
        bucket based on its value range. 
        3.  Sort all non-empty buckets.
        4.  Concatenate all buckets. 
    """